import { FigmaCollection } from "../../types";

const modes = ["primary", "secondary", "accent"];

/**
 * Target : brand colors from palette
 */

export const brandCollection: FigmaCollection = {
  name: "Brand",
  modes: modes,
  variables: {},
};

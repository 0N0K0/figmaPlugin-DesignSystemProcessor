import { FigmaCollection } from "../../types";

const modes = ["info", "success", "warning", "error"];

/**
 * Target : feedback colors from palette
 */

export const feedbackCollection: FigmaCollection = {
  name: "Feedback",
  modes: modes,
  variables: {},
};
